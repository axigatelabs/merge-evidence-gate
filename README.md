# merge-evidence-gate
Merge-Evidence Gate: re-runs AI-agent pull requests in a clean environment, compares what the agent claimed to what actually happened, and posts a signed receipt that decides merge eligibility.
