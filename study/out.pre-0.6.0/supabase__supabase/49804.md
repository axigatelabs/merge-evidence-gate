<!-- merge-evidence-gate -->
**Merge-Evidence Gate — FAIL**  (head f4ea2e2)

**Claims vs observed**
- `vitest` — ran ✘  exit 1, 0 failed — 0 introduced by this PR, 0 also failing at base bf60e6c
- runner: running the command the PR claimed (c1): `vitest`
- runner: running the claimed command in 3 workspace package(s) this PR touches: apps/studio, packages/common, packages/ui
- runner: merged 3 per-package report(s) found below the work dir (monorepo)
- baseline: runner: running the claimed command in 3 workspace package(s) this PR touches: apps/studio, packages/common, packages/ui
- baseline: runner: merged 3 per-package report(s) found below the work dir (monorepo)

**Verification layer**
- ✔ no skip/only markers added
- ✔ lockfile install OK

**Scope**
- · 7 changed files not mentioned in the PR body — apps/studio/components/interfaces/Account/Preferences/ThemeSettings.tsx, apps/studio/components/interfaces/App/ThemeOverridesProvider.tsx, apps/studio/hooks/misc/useThemeOverrides.ts … and 4 more

Details: receipt.json (artifact) · rerun: `f=0; (cd 'apps/studio' && export PATH="$PWD/node_modules/.bin:$PATH" && mkdir -p .merge-evidence && vitest --reporter=json --outputFile=.merge-evidence/vitest-results.json) || f=1; (cd 'packages/common' && export PATH="$PWD/node_modules/.bin:$PATH" && mkdir -p .merge-evidence && vitest --reporter=json --outputFile=.merge-evidence/vitest-results.json) || f=1; (cd 'packages/ui' && export PATH="$PWD/node_modules/.bin:$PATH" && mkdir -p .merge-evidence && vitest --reporter=json --outputFile=.merge-evidence/vitest-results.json) || f=1; exit "$f"` · 2m25s