<!-- merge-evidence-gate -->
**Merge-Evidence Gate — PASS**  (head 73d14e7)

**Claims vs observed**
- `vitest` — ran ✘  exit 1, 23 failed — all also fail at base aeb9511; nothing introduced by this PR
- runner: running the command the PR claimed (c1): `vitest`
- runner: running the claimed command in 5 workspace package(s) this PR touches: apps/studio, packages/icons, packages/api-types, apps/docs, packages/common
- runner: merged 3 per-package report(s) found below the work dir (monorepo)
- baseline: runner: running the claimed command in 5 workspace package(s) this PR touches: apps/studio, packages/icons, packages/api-types, apps/docs, packages/common
- baseline: runner: merged 3 per-package report(s) found below the work dir (monorepo)

**Verification layer**
- ✔ no skip/only markers added
- ✔ lockfile install OK

**Scope**
- · 15 changed files not mentioned in the PR body — apps/docs/content/guides/platform/upgrading.mdx, apps/studio/components/interfaces/Functions/httpHeaderAddActions.ts, apps/studio/components/interfaces/Workers/Workers.types.ts … and 12 more

Details: receipt.json (artifact) · rerun: `f=0; (cd 'apps/studio' && export PATH="$PWD/node_modules/.bin:$PATH" && mkdir -p .merge-evidence && vitest --reporter=json --outputFile=.merge-evidence/vitest-results.json) || f=1; (cd 'packages/icons' && export PATH="$PWD/node_modules/.bin:$PATH" && mkdir -p .merge-evidence && vitest --reporter=json --outputFile=.merge-evidence/vitest-results.json) || f=1; (cd 'packages/api-types' && export PATH="$PWD/node_modules/.bin:$PATH" && mkdir -p .merge-evidence && vitest --reporter=json --outputFile=.merge-evidence/vitest-results.json) || f=1; (cd 'apps/docs' && export PATH="$PWD/node_modules/.bin:$PATH" && mkdir -p .merge-evidence && vitest --reporter=json --outputFile=.merge-evidence/vitest-results.json) || f=1; (cd 'packages/common' && export PATH="$PWD/node_modules/.bin:$PATH" && mkdir -p .merge-evidence && vitest --reporter=json --outputFile=.merge-evidence/vitest-results.json) || f=1; exit "$f"` · 8m16s