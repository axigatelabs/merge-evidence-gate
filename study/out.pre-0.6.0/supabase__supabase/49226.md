<!-- merge-evidence-gate -->
**Merge-Evidence Gate — FAIL**  (head 93a5a2e)

**Claims vs observed**
- runner: root has no test command; running the test script of 2 workspace package(s) this PR touches: apps/studio, apps/www
- runner: merged 2 per-package report(s) found below the work dir (monorepo)

**Verification layer**
- ✘ 3 test files deleted in this PR — apps/studio/components/interfaces/Settings/Logs/LegacyLogsRewriteBanner.test.tsx, apps/studio/evals/scorer.test.ts, apps/studio/evals/transcript.test.ts
- ✔ no skip/only markers added
- ✔ lockfile install OK

**Scope**
- · 13 changed files not mentioned in the PR body — apps/studio/components/interfaces/Explorer/QueryEditor/QueryResultRenderer.tsx, apps/studio/components/interfaces/Explorer/QueryEditor/index.tsx, apps/studio/components/interfaces/SQLEditor/LegacyLogsRewriteBanner.tsx … and 10 more

Details: receipt.json (artifact) · rerun: `f=0; (cd 'apps/studio' && export PATH="$PWD/node_modules/.bin:$PATH" && mkdir -p .merge-evidence && pnpm test --reporter=json --outputFile=.merge-evidence/vitest-results.json) || f=1; (cd 'apps/www' && export PATH="$PWD/node_modules/.bin:$PATH" && mkdir -p .merge-evidence && pnpm test --reporter=json --outputFile=.merge-evidence/vitest-results.json) || f=1; exit "$f"` · 2m33s