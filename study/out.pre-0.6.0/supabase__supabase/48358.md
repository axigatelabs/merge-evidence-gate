<!-- merge-evidence-gate -->
**Merge-Evidence Gate — PASS**  (head 93ed06f)

**Claims vs observed**
- runner: root has no test command; running the test script of 1 workspace package(s) this PR touches: apps/www
- runner: merged 1 per-package report(s) found below the work dir (monorepo)

**Verification layer**
- ✔ no skip/only markers added
- ✔ lockfile install OK

**Scope**
- · 2 changed files not mentioned in the PR body — apps/www/lib/careers.ts, apps/www/pages/careers.tsx

Details: receipt.json (artifact) · rerun: `f=0; (cd 'apps/www' && export PATH="$PWD/node_modules/.bin:$PATH" && mkdir -p .merge-evidence && pnpm test --reporter=json --outputFile=.merge-evidence/vitest-results.json) || f=1; exit "$f"` · 1s