<!-- merge-evidence-gate -->
**Merge-Evidence Gate — FAIL**  (head 4bc977d)

**Claims vs observed**
- `pnpm test:studio` — ran ✘  exit 1, 0 failed
- runner: running the command the PR claimed (c1): `pnpm test:studio`
- runner: running the claimed command in 1 workspace package(s) this PR touches: e2e/studio; no machine-readable reporter could be injected into this command; per-test evidence is unavailable.
- baseline: dependency manifests differ between base and head and installs are disabled; the head failure could not be compared against base 9587eee

**Verification layer**
- ✔ no skip/only markers added
- ✔ lockfile install OK

Details: receipt.json (artifact) · rerun: `f=0; (cd 'e2e/studio' && export PATH="$PWD/node_modules/.bin:$PATH" && mkdir -p .merge-evidence && pnpm test:studio) || f=1; exit "$f"` · 0s