<!-- merge-evidence-gate -->
**Merge-Evidence Gate — PASS**  (head ade4030)

**Claims vs observed**
- the suite fails at head (4 failed) and at base f1e0808 (0 failed): 4 introduced by this PR
- runner: root has no test command; running the test script of 1 workspace package(s) this PR touches: apps/studio
- runner: merged 1 per-package report(s) found below the work dir (monorepo)
- baseline: runner: root has no test command; running the test script of 1 workspace package(s) this PR touches: apps/studio
- baseline: runner: merged 1 per-package report(s) found below the work dir (monorepo)

**Verification layer**
- ✔ no skip/only markers added
- ✔ lockfile install OK

**Scope**
- · 3 changed files not mentioned in the PR body — apps/studio/lib/ai/ai-details.ts, apps/studio/lib/ai/generate-assistant-response.ts, apps/studio/pages/api/ai/sql/generate-v4.ts

Details: receipt.json (artifact) · rerun: `f=0; (cd 'apps/studio' && export PATH="$PWD/node_modules/.bin:$PATH" && mkdir -p .merge-evidence && pnpm test --reporter=json --outputFile=.merge-evidence/vitest-results.json) || f=1; exit "$f"` · 7m55s