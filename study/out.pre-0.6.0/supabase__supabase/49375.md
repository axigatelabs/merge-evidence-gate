<!-- merge-evidence-gate -->
**Merge-Evidence Gate — FAIL**  (head 23e750a)

**Claims vs observed**
- `vitest` — ran ✔  5791/5791 pass
- runner: running the command the PR claimed (c1): `vitest`
- runner: running the claimed command in 2 workspace package(s) this PR touches: apps/studio, packages/common
- runner: merged 2 per-package report(s) found below the work dir (monorepo)

**Verification layer**
- ✘ 3 test files deleted in this PR — apps/studio/hooks/misc/__tests__/useSiwcQueryParamOptIn.test.ts, apps/studio/tests/pages/sign-in.test.tsx, apps/studio/tests/pages/sign-up.test.tsx
- ✔ no skip/only markers added
- ✔ lockfile install OK

Details: receipt.json (artifact) · rerun: `f=0; (cd 'apps/studio' && export PATH="$PWD/node_modules/.bin:$PATH" && mkdir -p .merge-evidence && vitest --reporter=json --outputFile=.merge-evidence/vitest-results.json) || f=1; (cd 'packages/common' && export PATH="$PWD/node_modules/.bin:$PATH" && mkdir -p .merge-evidence && vitest --reporter=json --outputFile=.merge-evidence/vitest-results.json) || f=1; exit "$f"` · 3m9s