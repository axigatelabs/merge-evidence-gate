<!-- merge-evidence-gate -->
**Merge-Evidence Gate — PASS**  (head 2664d17)

**Claims vs observed**
- "0 errors" — counts match ✔
- "26 passing" — unverifiable
- runner: root has no test command; running the test script of 1 workspace package(s) this PR touches: apps/studio
- runner: merged 1 per-package report(s) found below the work dir (monorepo)

**Verification layer**
- ✔ no skip/only markers added
- ✔ lockfile install OK

**Scope**
- · 3 changed files not mentioned in the PR body — apps/studio/components/interfaces/Settings/Infrastructure/InfrastructureConfiguration/HaTopology.utils.ts, apps/studio/data/ha-admin/ha-cluster-poolers-query.ts, apps/studio/data/ha-admin/keys.ts

Details: receipt.json (artifact) · rerun: `f=0; (cd 'apps/studio' && export PATH="$PWD/node_modules/.bin:$PATH" && mkdir -p .merge-evidence && pnpm test --reporter=json --outputFile=.merge-evidence/vitest-results.json) || f=1; exit "$f"` · 2m54s