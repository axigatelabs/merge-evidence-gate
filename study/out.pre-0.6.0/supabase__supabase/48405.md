<!-- merge-evidence-gate -->
**Merge-Evidence Gate — PASS**  (head bd72acb)

**Claims vs observed**
- `vitest run lib/sentry-client-options.test.ts` — unverifiable
- "47 tests" — counts match ✔
- runner: running the command the PR claimed (c1): `vitest run lib/sentry-client-options.test.ts`
- runner: running the claimed command in 1 workspace package(s) this PR touches: apps/studio
- runner: merged 1 per-package report(s) found below the work dir (monorepo)

**Verification layer**
- ✔ no skip/only markers added
- ✔ lockfile install OK

Details: receipt.json (artifact) · rerun: `f=0; (cd 'apps/studio' && export PATH="$PWD/node_modules/.bin:$PATH" && mkdir -p .merge-evidence && vitest run lib/sentry-client-options.test.ts --reporter=json --outputFile=.merge-evidence/vitest-results.json) || f=1; exit "$f"` · 1s