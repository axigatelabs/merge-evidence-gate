<!-- merge-evidence-gate -->
**Merge-Evidence Gate — PASS**  (head e6c6517)

**Claims vs observed**
- the suite fails at head (11 failed) and at base 9952d6f (11 failed): nothing introduced by this PR
- runner: root has no test command; running the test script of 3 workspace package(s) this PR touches: apps/studio, apps/docs, packages/ui-patterns
- runner: merged 3 per-package report(s) found below the work dir (monorepo)
- baseline: runner: root has no test command; running the test script of 3 workspace package(s) this PR touches: apps/studio, apps/docs, packages/ui-patterns
- baseline: runner: merged 3 per-package report(s) found below the work dir (monorepo)

**Verification layer**
- ✔ no skip/only markers added
- ✔ lockfile install OK

**Scope**
- · 11 changed files not mentioned in the PR body — apps/studio/components/interfaces/Integrations/VercelGithub/ProjectLinker.tsx, apps/studio/components/interfaces/Integrations/VercelGithub/VercelGithub.types.ts, apps/studio/components/interfaces/Organization/IntegrationSettings/SidePanelVercelProjectLinker.tsx … and 8 more

Details: receipt.json (artifact) · rerun: `f=0; (cd 'apps/studio' && export PATH="$PWD/node_modules/.bin:$PATH" && mkdir -p .merge-evidence && pnpm test --reporter=json --outputFile=.merge-evidence/vitest-results.json) || f=1; (cd 'apps/docs' && export PATH="$PWD/node_modules/.bin:$PATH" && mkdir -p .merge-evidence && pnpm run test:local --reporter=json --outputFile=.merge-evidence/vitest-results.json) || f=1; (cd 'packages/ui-patterns' && export PATH="$PWD/node_modules/.bin:$PATH" && mkdir -p .merge-evidence && pnpm test --reporter=json --outputFile=.merge-evidence/vitest-results.json) || f=1; exit "$f"` · 2m57s