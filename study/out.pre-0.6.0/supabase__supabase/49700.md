<!-- merge-evidence-gate -->
**Merge-Evidence Gate — PASS**  (head d157fa4)

**Claims vs observed**
- the suite fails at head (11 failed) and at base 86c813e (11 failed): nothing introduced by this PR
- runner: root has no test command; running the test script of 1 workspace package(s) this PR touches: apps/docs; the `test` script chains to `test:local`; running that step directly
- runner: merged 1 per-package report(s) found below the work dir (monorepo)
- baseline: runner: root has no test command; running the test script of 1 workspace package(s) this PR touches: apps/docs; the `test` script chains to `test:local`; running that step directly
- baseline: runner: merged 1 per-package report(s) found below the work dir (monorepo)

**Verification layer**
- ✔ no skip/only markers added
- ✔ lockfile install OK

Details: receipt.json (artifact) · rerun: `f=0; (cd 'apps/docs' && export PATH="$PWD/node_modules/.bin:$PATH" && mkdir -p .merge-evidence && pnpm run test:local --reporter=json --outputFile=.merge-evidence/vitest-results.json) || f=1; exit "$f"` · 46s