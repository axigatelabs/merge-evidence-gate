<!-- merge-evidence-gate -->
**Merge-Evidence Gate — PASS**  (head f7021f4)

**Claims vs observed**
- `vitest --run components/interfaces/Observability/DatabaseConnections` — ran ✘  exit 1, 0 failed — all also fail at base 60be899; nothing introduced by this PR
- "36 passed" — counts match ✔
- runner: running the command the PR claimed (c1): `vitest --run components/interfaces/Observability/DatabaseConnections`
- runner: running the claimed command in 2 workspace package(s) this PR touches: apps/studio, packages/common
- runner: merged 2 per-package report(s) found below the work dir (monorepo)
- baseline: runner: running the claimed command in 2 workspace package(s) this PR touches: apps/studio, packages/common
- baseline: runner: merged 2 per-package report(s) found below the work dir (monorepo)

**Verification layer**
- ✔ no skip/only markers added
- ✔ lockfile install OK

Details: receipt.json (artifact) · rerun: `f=0; (cd 'apps/studio' && export PATH="$PWD/node_modules/.bin:$PATH" && mkdir -p .merge-evidence && vitest --run components/interfaces/Observability/DatabaseConnections --reporter=json --outputFile=.merge-evidence/vitest-results.json) || f=1; (cd 'packages/common' && export PATH="$PWD/node_modules/.bin:$PATH" && mkdir -p .merge-evidence && vitest --run components/interfaces/Observability/DatabaseConnections --reporter=json --outputFile=.merge-evidence/vitest-results.json) || f=1; exit "$f"` · 2s