<!-- merge-evidence-gate -->
**Merge-Evidence Gate — FAIL**  (head 1bfe2cf)

**Claims vs observed**
- runner: root has no test command; running the test script of 2 workspace package(s) this PR touches: apps/studio, apps/www
- runner: merged 2 per-package report(s) found below the work dir (monorepo)

**Verification layer**
- ✘ 8 test files deleted in this PR — apps/studio/components/interfaces/Database/Publications/PublicationsAvailability.test.tsx, apps/studio/components/interfaces/Organization/Usage/Usage.test.tsx, apps/studio/components/interfaces/ProjectCreation/ProjectCreation.utils.test.ts … and 5 more
- ✔ no skip/only markers added
- ✔ lockfile install OK

**Scope**
- · 20 changed files not mentioned in the PR body — apps/studio/components/interfaces/Database/Publications/PublicationsAvailability.tsx, apps/studio/components/interfaces/Organization/Usage/Usage.tsx, apps/studio/components/interfaces/Project/ResumeProjectButton.tsx … and 17 more

Details: receipt.json (artifact) · rerun: `f=0; (cd 'apps/studio' && export PATH="$PWD/node_modules/.bin:$PATH" && mkdir -p .merge-evidence && pnpm test --reporter=json --outputFile=.merge-evidence/vitest-results.json) || f=1; (cd 'apps/www' && export PATH="$PWD/node_modules/.bin:$PATH" && mkdir -p .merge-evidence && pnpm test --reporter=json --outputFile=.merge-evidence/vitest-results.json) || f=1; exit "$f"` · 2m3s